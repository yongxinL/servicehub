############### NETWORKS ###############
networks:
    subnet:
        driver: bridge

############### SERVICES ###############
include:
    # --> infrastructure (infra)
    - compose/infra.yml

    # --> authentication (authn)
    - compose/authn.yml

    # --> web services (wbsvc)
    - compose/wbsvc.yml

    # --> agent services (agent)
    - compose/agent.yml
